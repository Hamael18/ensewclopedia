<div class="my-3">
    {{ form_start(form) }}
    {{ form_row(form.libelle) }}
    <div class="row">
        <div class="col-3">
            <h5>Difficulté</h5>
            {{ form_row(form.bool_level) }}
        </div>
        <div class="col-3">
            <h5>Longueur</h5>
            {{ form_row(form.bool_length) }}
        </div>
        <div class="col-3">
            <h5>Taille</h5>
            {{ form_row(form.bool_size) }}
        </div>
        <div class="col-3">
            <h5>Manche</h5>
            {{ form_row(form.bool_handle) }}
        </div>
    </div>
    <div class="row">
        <div class="col-4">
            <h5>Col</h5>
            {{ form_row(form.bool_collar) }}
        </div>
        <div class="col-4">
            <h5>Tissu</h5>
            {{ form_row(form.bool_fabric) }}
        </div>
        <div class="col-4">
            <h5>Style</h5>
            {{ form_row(form.bool_style) }}
        </div>
    </div>
    {{ form_row(form.logo) }}
    <div class="d-flex justify-content-center">
        <button type="submit" class="btn mb-5">{{ bouton_label }}</button>
    </div>
    {{ form_end(form) }}
</div>
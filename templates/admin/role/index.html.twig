{% extends 'bo_partials/layout.html.twig' %}

{% block title %}Liste des rôles{% endblock %}

{% block content %}
    <div class="row teal-text darken-4 mb-5"><h3>Liste des rôles</h3></div>
    <div class="my-3">
        <table class="table table-hover table-striped">
            <thead class="teal lighten-4 h5">
            <tr>
                <th class="align-middle col-1">
                    <a href="{{ path('admin_role_add') }}" class="btn waves-effect waves-light"><i class="fas fa-plus"></i></a>
                </th>
                <th class="align-middle">Rôle</th>
                <th class="col-4 align-middle"></th>
            </tr>
            </thead>
            <tbody>
            {% for role in roles %}
                <tr>
                    <td class="align-middle"></td>
                    <td class="align-middle">{{ role }}</td>
                    <td class="align-middle">
                        <a href="{{ path('admin_role_edit', {'id': role.id}) }}" class="btn teal lighten-4 teal-text text-darken-2"><i class="fas fa-wrench"></i></a>
                        <a href="{{ path('admin_role_delete', {'id': role.id }) }}"
                           onclick="return confirm('Supprimer ce rôle ?')"
                           class="btn btn-flat grey-text text-lighten-1"><i class="fa fa-trash"></i></a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

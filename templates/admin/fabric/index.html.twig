{% extends 'bo_partials/layout.html.twig' %}

{% block title %}Liste des tissus{% endblock %}

{% block content %}
    <div class="row teal-text darken-4 mb-5"><h3>Tissus disponibles</h3></div>
    <div class="my-3">
        <table class="table table-hover table-striped">
            <thead class="teal lighten-4 h5">
            <tr>
                <th class="align-middle col-1">
                    <a href="{{ path('admin_fabric_new') }}" class="btn waves-effect waves-light"><i class="fas fa-plus"></i></a>
                </th>
                <th class="align-middle">Nom</th>
                <th class="align-middle">Extensible</th>
                <th class="align-middle">Version(s)</th>
                <th class="col-2 align-middle"></th>
            </tr>
            </thead>
            <tbody>
            {% for fabric in data %}
                <tr>
                    <td class="align-middle"></td>
                    <td class="align-middle font-weight-bold h5">{{ fabric.name }}</td>
                    <td class="align-middle">{{ fabric.extensible }}</td>
                    <td class="align-middle">{{ fabric.versions | length }}</td>
                    <td class="align-middle">
                        <a href="{{ path('admin_fabric_edit', {'id': fabric.id}) }}" class="btn teal lighten-4 teal-text text-darken-2"><i class="fas fa-wrench"></i></a>
                        {% if fabric.canBeDeleted %}
                            <a href="{{ path('admin_fabric_delete', {'id': fabric.id }) }}"
                               onclick="return confirm('Supprimer ce tissu ?')"
                               class="btn btn-flat grey-text text-lighten-1"><i class="fa fa-trash"></i></a>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        {{ pagination.display }}
    </div>
{% endblock %}

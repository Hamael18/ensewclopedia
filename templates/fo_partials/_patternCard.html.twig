<div class="col-4">
    <div class="card teal lighten-4">
        <div class="card-image">
            <img src="/uploads/pattern_images/{{ pattern.image }}" alt="{{ pattern.name }}">
            <a href="#"
               class="btn-floating halfway-fab waves-effect waves-light teal"><i class="fas fa-heart"></i></a>
        </div>
        <div class="card-content">
            <span class="card-title center"><a href="#" class="teal-text text-darken-4">{{ pattern.name }}</a></span>
            <p class="teal-text text-darken-2">{{ pattern.description }}</p>
        </div>
    </div>
</div>
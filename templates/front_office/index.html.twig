{% extends 'fo_partials/base.html.twig' %}

{% block title %}Acceuil enSewClopedia{% endblock %}

{% block body %}

    {% include 'fo_partials/search.html.twig' %}
    <div class="row my-lg-5">
        <div class="container">
            <div class="row app">
                <h4 class="teal-text text-darken-4">Nouveautés</h4>
            </div>
            <div class="row my-3">
                {% for pattern in lastPatterns %}
                    <div class="col-4">
                        <div class="card medium teal lighten-4">
                            <div class="card-image responsive-img">
                                <img src="/uploads/brand_images/{{ pattern.image }}" alt="{{ pattern.name }}">
                                <a class="btn-floating halfway-fab waves-effect waves-light teal"><i class="fas fa-heart"></i></a>
                            </div>
                            <div class="card-content">
                                <span class="card-title center"><a href="#" class="white-text">{{ pattern.name }}</a></span>
                                <p class="white-text">{{ pattern.description }}</p>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>

{#    <div class="col-4">#}
{#        <div class="card teal darken-3">#}
{#            <div class="card-image">#}
{#                <img src="/uploads/brand_images/{{ pattern.image }}" alt="{{ pattern.name }}">#}
{#                <a href="#" title="ajouter aux favoris"#}
{#                   class="btn-floating halfway-fab waves-effect waves-light teal">#}
{#                    <i class="fas fa-heart"></i>#}
{#                </a>#}
{#            </div>#}
{#            <div class="card-content">#}
{#                <span class="card-title center"><a href="#" class="white-text">{{ pattern.name }}</a></span>#}
{#                <p class="white-text">{{ pattern.description }}</p>#}
{#            </div>#}
{#        </div>#}
{#    </div>#}

    <div class="row my-lg-5">
        <div class="container">
            <div class="row">
                <h4 class="teal-text text-darken-4">Populaires</h4>
            </div>
        </div>
    </div>

{% endblock %}